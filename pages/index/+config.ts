import type { Config } from 'vike/types';

export default {
	title: 'Welcome | cutler.so',
	description: 'A statically generated React multi-page application showcasing SSG capabilities',
	prerender: true,
} satisfies Config;