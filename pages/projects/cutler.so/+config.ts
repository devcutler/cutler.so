import type { Config } from 'vike/types';

export default {
  title: 'cutler.so | Projects',
  description: 'Personal website built with Vike, React, and TypeScript featuring SSG, multiple themes, and a markdown blog.'
} satisfies Config;